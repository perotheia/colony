# colony wiki

- **[OTA e2e CI — setup, flow, and strategy](OTA-e2e-CI)** — the nightly integration
  gate: build platform → S3 → provision split_rig from S3 → deliver user apps over
  Mender OTA, in containers, on every nightly. Strategy + CI setup + the bridge/TIPC
  model.
